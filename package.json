{
  "name": "macroecon-simulator",
  "version": "1.0.0",
  "description": "Interactive macroeconomic simulation game",
  "main": "index.html",
  "scripts": {
    "build": "tsc && cp index.html build/ && cp styles.css build/",
    "watch": "tsc --watch",
    "serve": "python3 -m http.server 8000",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/chart.js": "^2.9.0"
  }
}